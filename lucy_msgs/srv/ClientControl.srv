# Request
string client_id              # requesting client's unique id
bool acquire                  # true = take control, false = release control
---
# Response
bool success                  # whether the request was applied
string active_client          # resulting controller id ("" = nobody in control)
string message
