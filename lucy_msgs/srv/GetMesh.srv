# Request
string path                   # mesh reference from the URDF (file://, package://, or absolute)
---
# Response
bool success
string message
# Payload encoding for `data`:
#   utf8         — text mesh (Collada/DAE XML)
#   zlib_base64  — zlib-compressed binary (STL), then standard base64
string encoding
string data
