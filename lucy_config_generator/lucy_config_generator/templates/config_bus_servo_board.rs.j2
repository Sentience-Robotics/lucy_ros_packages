//! Per-board register layout. Generated by lucy_config_generator for {{ board_id }} — do not edit.

/// Registers per bus-servo block: id, angle, cmd.
pub const BUS_SERVO_BLOCK: u16 = {{ bus_servo_block }};

pub const BUS_SERVO_BASE: u16 = {{ bus_servo_base }};

/// Slots scanned each loop. Must cover the highest `virtual_pin` the host
/// assigns, and must not reach registers owned by PWM servos or sensors: a bus
/// adapter would read those as opcodes.
pub const BUS_SERVO_SLOTS: u16 = {{ bus_servo_slots }};
